# Templates for 01-ingest

| File | Purpose |
|---|---|
| `v1.json.template` | Optional JSON sidecar (text is the primary output as v1.txt) |
| `v1.meta.json.template` | (shared) Canonical meta block |

The primary artifact is `v1.txt` (plain UTF-8 text). The JSON sidecar `v1.json` is optional and carries metadata.
