# Figma to Webflow Build Assets

Store reusable static diagrams or QA screenshots here only when they are safe to commit.

Do not store private client screenshots unless the project policy allows it.

