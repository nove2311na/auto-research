# Scripts

Helper scripts for the parallel-section-build skill.

- `scripts/gates/validate_build_contract.py` - validates blueprint build contracts before any write.
- `scripts/sync_library_to_webflow.py` - creates per-project classes on Webflow (Phase 2A class setup).
