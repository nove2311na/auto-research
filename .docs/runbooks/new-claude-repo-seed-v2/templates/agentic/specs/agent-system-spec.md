# Agent System Spec

Store the generated `agent_system_spec` here.

## Summary

Record the normalized idea, domain, target runtime, stack, risk level, and assumptions.

## Agents

Each agent must include role, trigger, allowed tools, forbidden actions, input contract, output contract, stop conditions, and escalation.

## Skills

Each skill must include trigger, workflow, references, scripts, assets, and validation.

## Tools and MCP Servers

Each tool and MCP server must include purpose, risk class, approval policy, auth requirements, allowed agents, and validation.

## Workflows

Each workflow must include trigger, inputs, steps, outputs, retry limit, stop conditions, and validation gate.

## Memory

Separate durable knowledge, memory candidates, approved memory, and generated logs.

## Gates

List structure, quality, secret, schema, benchmark, and workflow gates.

## V1 Benchmark Alignment

Record how this repo matches or exceeds `.docs/runbooks/new-claude-repo/`.

